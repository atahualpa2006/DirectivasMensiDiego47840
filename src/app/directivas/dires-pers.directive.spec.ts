import { DiresPersDirective } from './dires-pers.directive';

describe('DiresPersDirective', () => {
  it('should create an instance', () => {
    const directive = new DiresPersDirective();
    expect(directive).toBeTruthy();
  });
});
